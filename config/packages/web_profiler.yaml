when@dev:
    web_profiler:
        toolbar: false
        intercept_redirects: false

    framework:
        profiler:
            collect: false
            only_main_requests: true
            collect_serializer_data: false

when@test:
    framework:
        profiler: { collect: false }
